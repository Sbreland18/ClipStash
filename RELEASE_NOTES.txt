Version 1.1.0

- Your settings are now remembered between launches
- Download queue: add several links and leave it running
- Retry failed: puts anything that didn't finish back in the queue
- Activity is recorded to a log file, reachable via the Open logs button
- Paste button, Enter to start, and drag a link onto the window
